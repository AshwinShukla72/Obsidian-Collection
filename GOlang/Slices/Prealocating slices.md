```ad-info
Slices can be pre-allocated with specific capabilities
To prealloace a slice `make()` is used
This practice is useful when number of elements is known but, their values are unknown 
`slice := make([]int,10)` Make a slice of capacity 5
```
